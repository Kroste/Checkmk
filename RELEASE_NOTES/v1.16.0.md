Das Repo ist umgezogen: von `github.com/Kroste/Checkmk` in die neue
Organisation **[LHP542](https://github.com/LHP542)**, unter der ab jetzt alle
dienstlichen Projekte der **Arbeitsgruppe 5424 IT-Basis-Dienste** liegen.

## Was Du merkst

**Nichts, was Du tun müsstest.** Die alten Adressen leiten weiter, und der
Update-Kanal läuft ohnehin über den Fileshare
`\\samba01\542$\...\CheckMK_Copilot`.

Angepasst sind:

- der **GitHub-Knopf** in der Über-Box
- die Angabe **Fachbereich** → **Arbeitsgruppe 5424 IT-Basis-Dienste**
- die Vorgabe des Update-Kanals (`api.github.com/repos/LHP542/Checkmk/...`) —
  greift nur, wenn in der Datenbank keine Adresse hinterlegt ist
- alle Links in Handbuch und Entwicklerdoku
- die beiden Plugin-Submodule, die ebenfalls umgezogen sind

## Für Entwickler

Die Submodul-Adressen in `.gitmodules` zeigen jetzt auf `LHP542`. In einer
bestehenden Arbeitskopie einmal nachziehen:

```bash
git submodule sync --recursive
```

Ohne das bleibt die alte Adresse im lokalen `.git/config` stehen. Ein Fehler
entsteht dadurch nicht — GitHub leitet weiter —, aber sauber ist es so.

Das eigene `origin` genauso:

```bash
git remote set-url origin https://github.com/LHP542/Checkmk.git
```

## Nicht angefasst

Der lokale Datenordner `%APPDATA%\Kroste\Checkmk` bleibt **unverändert**. Ihn
umzubenennen würde jedem seine Einstellungen, Filter und Spaltenbreiten
wegnehmen, ohne irgendetwas zu verbessern — der Ordnername hat mit dem
GitHub-Konto nichts zu tun.

Ebenso bleiben die `Kroste*Brush`-Stilnamen im XAML und der Verweis auf den
`kroste-avalonia`-Skill: Der ist ein persönliches Repo und zieht nicht mit um.

Die Links in älteren Release-Notes zeigen dagegen **mit** auf die neue Adresse.
Sie beschreiben zwar einen alten Stand, verweisen aber auf *dieses* Projekt —
und das liegt jetzt woanders.
